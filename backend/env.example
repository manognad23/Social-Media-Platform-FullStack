PORT=5000
MONGO_URI=mongodb://localhost:27017/spotmies
JWT_SECRET=change_me_super_secret

# OpenAI Moderation API
OPENAI_API_KEY=

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional: comma-separated emails that should be admins (first login will promote if matched)
ADMIN_EMAILS=manognashree23@gmail.com

# Admin Secret Key (required for admin login)
ADMIN_SECRET=Manu4632

# CORS
CLIENT_ORIGIN=http://localhost:5173

# Email Notifications (Optional - for moderation removal emails)
# If not configured, email notifications will be skipped silently
# For Gmail: use App Passwords, not regular password
# For Other SMTP: replace with your provider's details
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
MAIL_FROM=noreply@spotmies.com

# Force flagging for testing (set to "true" to flag all posts/comments automatically)
# FORCE_FLAGGED=true
